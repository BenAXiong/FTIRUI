Phase 8 – Profile Dropdown & Page Plan
1. Refactor `header_nav.html` user status block into a Bootstrap dropdown: avatar becomes the toggle, showing the current user name/email plus menu entries (Profile, Settings placeholder, Sign out) while keeping Sign in visible for guests.
2. Wire the dropdown actions in `app.js`/workspace init so guest vs authenticated states toggle appropriately (show Sign in button vs dropdown), and ensure the “Profile” menu item navigates to a new `/profile/` route.
3. Implement a minimal `profile` view/template (requires login) showing user info (avatar, username, email, links to change password) and embed the existing Cloud State card (extract the `cloud_state_card` markup into a reusable partial so both dashboard + profile can include it).
4. Update `urls.py` to expose the profile route, add tests (template smoke + permission check), and mention the new dropdown/profile page in README so folks know how to reach cloud states outside the dashboard.

___________________________________________________________
Questions

*
___________________________________________________________
Polishing

- browser: consider floating draggable pill to avoid canvas resizing
- canvas: extract unredo from toolbar or extra unnumbered
- solve popup width (increase or auto)
- legend button state inits on on while legend is off
- remove screenshot icon from plotly area
    also zooms & "produced with...."
- fix browser popups 
- rethink plot types: defaults, changing, etc
- dash/explorer: extensible to the right?
- canvas thulbnails (2-3d???)
- dash main list ... popups: up and bottom position out of bounds
- login area: move "Sign out" into dropdown, arrange order with d/lmode, load pic, etc
- Papers and reports?
- dash/Top band: rethink, avoid going too  big with new objetc types
- main view options - curate
- dash exp scrollbar position
- dash project icons
- dash exp: clearer pins
- dash main: extra column = contributors
___________________________________________________________
Future work

- ?hide other tabs for MVP and rework UI (miro-like tabless design?)
- research annotations
- EC : CV, LSV, 
- new tab: Present
- feature: in canvas' explorer - load raw files and display sparklines on hover
- import opj and opju

Bugs

- grids: minor become major when major turned off
- browser: styling chips, only thickness wired
