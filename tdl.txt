Phase 9 – Dashboard Template Refactor
1. Split `apps/ftirui/ft/templates/ft/workspace/panes/dashboard.html` into smaller partials (cloud card, list shell, Papers/Reports placeholders) so each component maintains its own markup balance.
2. Move the existing cloud-state section into the reusable partial (already shared with the profile page) and create similar includes for collapsed cards; add a simple lint script (e.g. Python div counter) to CI/TDL for future edits.
3. Ensure tab panes (`workspace`, `dashboard`, `live`, `tools`, `about`) remain direct children of `.tab-content` and document the structure in README/TDL to prevent regressions.
4. Run Vitest + Django tests after the refactor to confirm no behavioural changes; update `tdl.txt` once the refactor is complete.

___________________________________________________________
Questions

*
___________________________________________________________
Polishing

- browser: consider floating draggable pill to avoid canvas resizing
- canvas: extract unredo from toolbar or extra unnumbered
- solve popup width (increase or auto)
- legend button state inits on on while legend is off
- remove screenshot icon from plotly area
    also zooms & "produced with...."
- fix browser popups 
- rethink plot types: defaults, changing, etc
- dash/explorer: extensible to the right?
- canvas thumbnails (2-3d???)
- dash main list ... popups: up and bottom position out of bounds
- login area: move "Sign out" into dropdown, arrange order with d/lmode, load pic, etc
- Papers and reports?
- dash/Top band: rethink, avoid going too  big with new objetc types
- main view options - curate
- dash exp scrollbar position
- dash project icons
- dash exp: clearer pins
- dash main: extra column = contributors
___________________________________________________________
Future work

- ?hide other tabs for MVP and rework UI (miro-like tabless design?)
- research annotations
- EC : CV, LSV, 
- new tab: Present
- feature: in canvas' explorer - load raw files and display sparklines on hover
- import opj and opju

Bugs

- grids: minor become major when major turned off
- browser: styling chips, only thickness wired
